export interface SocialLink {
  name: string;
  url: string;
  icon: string;
  color: string;
}

export interface Skill {
  name: string;
  icon: string;
}

export interface GalleryItem {
  id: number;
  url: string;
}
